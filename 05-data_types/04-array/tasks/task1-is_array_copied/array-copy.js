let fruits = ["Apples", "Pear", "Orange"];
let shoppingCart = fruits; // create a reference

// push a new value into the "copy"
shoppingCart.push("Banana");

// what's in fruits?
alert(`Fruits length: ${fruits.length}`); // 4
