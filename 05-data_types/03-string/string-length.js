alert(`My\n`.length); // 3

// NOTE: \n is a single "special" character, so the length is indeed 3.
// NOTE: length is a property: str.length is a numeric property, not a
// function. There is no need to add parenthesis after it. Not .length(), but
// .length.
